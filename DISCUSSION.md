Key Changes:

- Create concrete Advocate model for prop type checking
- Add missing table elements 
- Extract table into reusable components
- Remove case-sensitivity from search filter
- Add logs and error handling to fetch request
- Extract hooks and utility functions for reusability
- Created subfolders to organize and structure code
- Install and use Material UI components
- Replace table with Material UI data table component
- Add an app tool bar 

Future Enhancements:

- Improve styling of the data table 
- Optimize the search/filter logic - map the advocate fields to a list or dictionary and use native JS functions to check if list contains search value
